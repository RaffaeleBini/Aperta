services:
  aperta:
    build:
      context: .
      args:
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
    ports:
      - "3000:3000"
    environment:
      - SENTRY_DSN=${SENTRY_DSN:-}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
